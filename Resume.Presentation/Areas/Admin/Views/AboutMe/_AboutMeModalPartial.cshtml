@model Resume.Domain.ViewModels.AboutMe.CreateOrEditAboutMeViewModel

@{
    bool isCreate = Model.Id == 0 ? true : false;
}


<form id="AboutMeForm" asp-area="Admin" asp-controller="AboutMe" asp-action="SubmitAboutMeFormModal"
      data-ajax="true" data-ajax-begin="StartLoading()" data-ajax-success="AboutMeFormSubmited">

    <div class="row">
        <div class="col-12">
            @if (!isCreate)
            {
                <input type="hidden" asp-for="Id" />
            }

            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">@(isCreate ? "افزودن" : "ویرایش")</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">

                <div class="form-group">
                    <label asp-for="title"></label>
                    <div class="controls">
                        <input type="text" asp-for="title" class="form-control">
                    </div>
                    <span asp-validation-for="title" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="paragraf1"></label>
                    <div class="controls">
                        <textarea asp-for="paragraf1" class="form-control">

                        </textarea>
                    </div>
                    <span asp-validation-for="paragraf1" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="paragraf2"></label>
                    <div class="controls">
                        <textarea asp-for="paragraf2" class="form-control">

                        </textarea>
                    </div>
                    <span asp-validation-for="paragraf2" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary @(isCreate ? "btn-success" : "btn-warning")">@(isCreate ? "افزودن" : "ویرایش")</button>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">خروج</button>
                </div>
            </div>




        </div>
    </div>


</form>



