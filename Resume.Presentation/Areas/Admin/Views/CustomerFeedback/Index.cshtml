@model List<CustomerFeedbackViewModel>
@using Resume.Domain.ViewModels.CustomerFeedback
@using Resume.Application.StaticTools

@{
    ViewData["Title"] = "لیست نظرات";
}


<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="d-flex align-items-center">
        <div class="mr-auto">
            <h3 class="page-title">@ViewData["Title"]</h3>
        </div>
        <div class="right-title">
            <!-- Button trigger modal -->
            <button type="button" onclick="LoadCustomerFeedbackFormModal(0)" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter">
                افزودن به لیست
            </button>
        </div>

    </div>
</div>

<!-- Main content -->
<section class="content">

    <div class="row">
        <div class="col-12">
            <div class="box">
                <div class="box-body">
                    <div class="table-responsive">
                        <table class="table table-hover no-wrap data_table" id="data-table-box">
                            <thead>
                                <tr class="bg-secondary">
                                    <th>آواتار</th>
                                    <th>نام</th>
                                    <th>حوضه کاری</th>
                                    <th>الویت</th>
                                    <th>توضیحات</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>

                                @if (Model != null && Model.Any())
                                {
                                    @foreach (CustomerFeedbackViewModel item in Model)
                                    {
                                        <tr id="list-thing-@item.Id">
                                            <td><img src="@(string.IsNullOrEmpty(item.Avatar) ? FilePaths.DefaultAvatar : $"{FilePaths.CustomerFeedbackAvatar}{item.Avatar}")" width="70" height="70" /></td>
                                            <td>@item.Name</td>
                                            <td>@item.AboutJob</td>
                                            <td>@item.Order</td>
                                            <td>@item.Description</td>
                                            <td>

                                                <button onclick="LoadCustomerFeedbackFormModal(@item.Id)" type="button" class="btn btn-warning">ویرایش</button>
                                                <button onclick="DeleteCustomerFeedback(@item.Id)" type="button" class="btn btn-danger">حذف</button>

                                            </td>
                                        </tr>
                                    }
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

</section>
<!-- /.content -->
