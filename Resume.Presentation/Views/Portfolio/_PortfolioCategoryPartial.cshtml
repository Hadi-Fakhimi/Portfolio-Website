@using Resume.Domain.ViewModels.PortfolioCategory
@model List<PortfolioCategoryViewModel>

<div class="button-group">
    <button type="button" class="btn btn-light active" id="btn-1">
        همه
    </button>
    @foreach(PortfolioCategoryViewModel item in Model)
    {
        <button type="button" class="btn btn-light" data-filter=".@item.Name">
            @item.Title
        </button>
    }

</div>